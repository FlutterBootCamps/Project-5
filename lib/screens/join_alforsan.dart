import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class JoinAlForsan extends StatelessWidget {
  const JoinAlForsan({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
      body:
      const Center(child: Text("AlForsan")), //
    );
  }
}